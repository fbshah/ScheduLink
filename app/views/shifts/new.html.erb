<% if can? :manage, :all %>

<div class = "row text-center">
	<div class = "large-6 large-centered columns">
		<div id = "shifts">
			<h1>New Shift</h1>
				<%= render 'form' %>
			<%= link_to 'Back', shifts_path %>
		</div>
	</div>
</div>
<% end %>	